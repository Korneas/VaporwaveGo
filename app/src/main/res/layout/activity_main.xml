<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.camilomontoya.vaporwavego.MainActivity">

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/right_click"
        android:id="@+id/superior"
        android:background="#00000000"
        android:scaleX="0.8"
        android:scaleY="0.8"
        android:layout_marginBottom="14dp"
        android:layout_above="@+id/inferior"
        android:layout_centerHorizontal="true"
        android:contentDescription="Boton superior" />

    <ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:srcCompat="@drawable/down_click"
        android:id="@+id/inferior"
        android:scaleX="0.8"
        android:scaleY="0.8"
        android:background="#00000000"
        android:layout_marginBottom="14dp"
        android:layout_alignParentBottom="true"
        android:layout_alignStart="@+id/superior"
        android:contentDescription="Boton inferior" />
</RelativeLayout>
